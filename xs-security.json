{
  "xsappname": "stdui5",
  "tenant-mode": "dedicated",
  "description": "Security profile of called application",
  "oauth2-configuration": {
    "redirect-uris": [
      "https://*.hana.ondemand.com/**",
      "https://*.trial.applicationstudio.cloud.sap/login/callback"
    ]
  },
  "scopes": [
    {
      "name": "$XSAPPNAME.accessDataService",
      "description": "Allows to access data service within ui5-standalone app"
    },
    {
      "name": "$XSAPPNAME.accessApp",
      "description": "Allows to generally access ui5-standalone app"
    }
  ],
  "role-templates": [
    {
      "name": "STDUI5_Role",
      "description": "Role Template for Standalone App Router Testing",
      "scope-references": [
        "$XSAPPNAME.accessApp",
        "$XSAPPNAME.accessDataService"
      ]
    }
  ]
}